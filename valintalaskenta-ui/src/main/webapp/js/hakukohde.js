app.factory('HakukohdeModel', function() {

	
});

function HakukohdeController($scope, $location, $routeParams) {
	//$scope.model = HakukohdeModel;
	//console.log("current hakuoid: " + $routeParams.hakuOid);
	//$scope.model.refreshIfNeeded($routeParams.hakuOid);
}